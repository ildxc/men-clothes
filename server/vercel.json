{
    "version": 2,
    "builds": [
        {
            "src": "index.js",
            "use": "@vercel/node",
            "config": { "includeFiles": ["/**"] }
        }
    ],
    "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
